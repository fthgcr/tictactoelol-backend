server.tomcat.accesslog.enabled=true

# No datasource: champion data is a static JSON resource (data/champions.json)
# and game sessions are held in memory by SessionRepository.

# In-memory session TTL (see SessionCleanupService)
# How long a finished match is kept so both clients can still read the final board
game.session.finished-ttl=PT30M
# How long an untouched session is kept (abandoned games, unclaimed matchmaking seats)
game.session.idle-ttl=PT2H
# How often the cleanup job runs, in milliseconds
game.session.cleanup-interval-ms=600000

# Turn watchdog (see TurnWatchdogService): ends turns a departed player can no longer end
# How long a turn may go unanswered - must stay above the client's own 30s countdown
game.turn.timeout=PT38S
# Unanswered turns by the same player before the opponent is handed the win
game.turn.max-missed=2
# How often the watchdog checks, in milliseconds
game.turn.watchdog-interval-ms=5000

# WebSocket endpoint
spring.websocket.path=/ws

# WebSocket broker configuration
spring.websocket.messaging.path=/app
spring.websocket.messaging.user-destination-prefix=/user
spring.websocket.messaging.stomp.endpoint=/ws
spring.websocket.messaging.stomp.allowed-origins=*

# Enable/disable debug logging for WebSocket
logging.level.org.springframework.web.socket=DEBUG

# Skin counts (see ChampionSkinDataService). Unlike every other champion attribute these
# change every patch, so they are fetched from Riot's Data Dragon instead of being stored
# in champions.json. Until the first fetch succeeds the skin rule category is simply left
# out of the games - set enabled=false to keep it out permanently.
game.skins.enabled=true
# Delay before the first fetch; keeps it off the startup path
game.skins.initial-delay-ms=2000
# How often to re-fetch, in milliseconds (6 hours) - also picks up newly released skins
game.skins.refresh-interval-ms=21600000
# Per-request connect/read timeout
game.skins.request-timeout=PT10S
